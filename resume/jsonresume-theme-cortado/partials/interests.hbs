{{#if resume.interests.length}}
<div class="container interests-container">
  {{> title value="Research Interests"}}

  {{#each resume.interests}}
    <section class="item">
      <ul class="minimal">
        <li>{{.}}</li>
      </ul>
    </section>
  {{/each}}
</div>
{{/if}}
